specVersion: "v1"
serviceId: "agenda"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "agenda-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
